#pragma once

#include "%moduleHeader"

#include "Server/BaseServer.h"

namespace %game {
	namespace %module {

		class %guard %name : public Engine::Server::BaseServer {
		public:
			%name();
		};

	}
}