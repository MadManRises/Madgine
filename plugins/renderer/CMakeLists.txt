
if (NOT NO_OPENGL_SUPPORT)
	add_subdirectory(opengl)
endif ()
if (NOT NO_VULKAN_SUPPORT)
	add_subdirectory(vulkan)
endif ()
if (WINDOWS)
	if (NOT NO_DIRECTX12_SUPPORT)
		add_subdirectory(directx12)
	endif()
endif ()
