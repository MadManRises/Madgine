#pragma once

namespace Engine {

namespace Widgets {
    struct WidgetManager;

	struct WidgetBase;
    struct Bar;
    struct Button;
    struct SceneWindow;
    struct Checkbox;
    struct Combobox;
    struct Textbox;
    struct Label;
    struct Image;
    struct TableWidget;
    struct TabBar;
    struct Layout;

    struct Geometry;
    struct GeometrySourceInfo;

    struct WidgetsRenderData;

    FORWARD_ENUM(WidgetClass);

}

}
