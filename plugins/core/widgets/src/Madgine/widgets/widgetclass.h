#pragma once

namespace Engine {
namespace Widgets {

    ENUM(WidgetClass,
        WIDGET,
        SCENEWINDOW,
        COMBOBOX,
        CHECKBOX,
        TEXTBOX,
        BUTTON,
        LABEL,
        BAR,
        IMAGE,
        TABLEWIDGET,
        TABBAR,
        TEXTEDIT,
        LAYOUT)
}
}
