#pragma once

namespace Engine {
namespace Widgets {

    ENUM(WidgetClass,
        WIDGET,
        SCENEWINDOW,
        COMBOBOX,
        TABWIDGET,
        CHECKBOX,
        TEXTBOX,
        BUTTON,
        LAYOUT,
        LABEL,
        BAR,
        IMAGE);
}
}
